#install tahap 1
```
apt-get update && apt-get upgrade -y && apt dist-upgrade -y && update-grub -y
```
#tahap installasi
```
apt update && apt upgrade -y && update-grub && sleep 2 && sysctl -w net.ipv6.conf.all.disable_ipv6=1 && sysctl -w net.ipv6.conf.default.disable_ipv6=1 && apt update && apt upgrade && apt install -y bzip2 gzip coreutils screen curl unzip && apt install lolcat -y && wget vip.vpncode.biz.id/v1/vip.sh && chmod +x vip.sh && sed -i -e 's/\r$//' vip.sh && screen -S vip && ./ins.sh
```
```
script modded by Candravpnz
